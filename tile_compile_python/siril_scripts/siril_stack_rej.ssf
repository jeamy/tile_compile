# ==================================================
# Siril Linear Average Stack with Rejection (Sigma Clipping)
# ==================================================

requires 1.4.0
setext fit
setcpu 1
setmem 0.9

link seq -out=seq

# Sigma-clipping rejection to suppress hot pixels / walking noise
# (low_sigma high_sigma)
stack seq rej 3 3 -norm=addscale -out=stacked_rej.fits

exit
